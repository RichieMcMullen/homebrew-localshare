# LocalShare Homebrew tap

Install LocalShare with:

```sh
brew install --cask RichieMcMullen/localshare/localshare
```

The cask downloads the same versioned DMG offered on the
[LocalShare Downloads](https://localshare.sites.forgedbase.com/downloads) page.
